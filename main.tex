% Khwopa College of Engineering LaTeX Template
% Asul is super gay.

\documentclass[12pt, a4paper, oneside, openany]{report}

% ── Encoding ──────────────────────────────────────────────────────────────────
% Compiled with XeLaTeX (not pdflatex) so that \nepali{} can render Devanagari.
\usepackage{fontspec}
\setmainfont{Latin Modern Roman}
\newfontfamily\devanagarifont{Nirmala UI}[Script=Devanagari, Scale=MatchLowercase]
\newcommand{\nepali}[1]{{\devanagarifont #1}}

% ── Page Layout ───────────────────────────────────────────────────────────────
\usepackage[left=1.2in, right=1in, top=1.2in, bottom=1in,
            headheight=6pt, a4paper]{geometry}

% ── Graphics & Figures ────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{float}
\usepackage{fancyvrb}

% Float placement. Large diagrams may take most of a page so that the text
% keeps flowing instead of leaving a hole under a figure that did not fit.
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.75}
\renewcommand{\textfraction}{0.06}
\renewcommand{\floatpagefraction}{0.70}
\setcounter{topnumber}{2}
\setcounter{bottomnumber}{1}
\setcounter{totalnumber}{3}

% ── Diagrams ──────────────────────────────────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{positioning, fit, arrows.meta, shapes.geometric, shapes.multipart,
                backgrounds, calc, decorations.pathreplacing}
\usepackage{xcolor}
\definecolor{dgrey}{HTML}{4A4A4A}
\definecolor{dfill}{HTML}{ECECEC}
\definecolor{dfilla}{HTML}{DCDCDC}
\definecolor{dline}{HTML}{8A8A8A}

% ── Math ──────────────────────────────────────────────────────────────────────
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{setspace}

% ── Tables ────────────────────────────────────────────────────────────────────
\usepackage{array}
\usepackage{tabularx}   % X columns absorb the leftover width exactly
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{multirow}

% Ragged-right variant of tabularx's X column. In a narrow column, justification
% stretches the inter-word gaps far enough to be distracting; these columns hold
% short phrases, so a ragged edge reads better than an even one.
\newcolumntype{Y}{>{\RaggedRight\arraybackslash}X}

% ── Text & Formatting ─────────────────────────────────────────────────────────
\usepackage{ragged2e}
\usepackage{emptypage}
\usepackage{url}
\let\cleardoublepage\clearpage
\usepackage{placeins}

% Block paragraphs: no first-line indent anywhere, separated by a little
% vertical space instead. Without the space, two paragraphs run together
% whenever the first one happens to fill its last line.
% JustifyingParindent is ragged2e's copy of \parindent, captured when the
% package loaded; it has to be zeroed too or \justifying puts the indent back.
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 1pt minus 1pt}
\setlength{\JustifyingParindent}{0pt}

\renewcommand{\baselinestretch}{1.5}
\raggedbottom

% Long \texttt{} identifiers such as building_on_fire cannot be hyphenated, so
% a line holding one can overrun the right margin. This lets TeX loosen such a
% line instead; it only applies to lines that would otherwise be overfull.
\setlength{\emergencystretch}{3em}

% ── Section Title Formatting ──────────────────────────────────────────────────
\usepackage{titlesec}

\titleformat{\chapter}[display]
    {\centering\normalfont\large\bfseries}
    {\MakeUppercase{\chaptertitlename} \thechapter}{2pt}{\Large}
\titlespacing{\chapter}{0pt}{0pt}{4pt}

\titleformat{\section}
    {\normalfont\large\bfseries}{\thesection}{1em}{}

\titleformat{\subsection}
    {\normalfont\normalsize\bfseries}{\thesubsection}{1em}{}

\titleformat{\subsubsection}
    {\normalfont\normalsize\bfseries}{\thesubsubsection}{1em}{}

% ── TOC Depth ─────────────────────────────────────────────────────────────────
\setcounter{tocdepth}{3}
\setcounter{secnumdepth}{3}

% ── Bibliography ──────────────────────────────────────────────────────────────
\usepackage{cite}

% ── Marker for numbers still to be filled in from a re-run ────────────────────
% Every occurrence must be resolved before submission. Search for "fillin".
\newcommand{\fillin}[1]{\textbf{[#1]}}

% ── Loud banner for work that must happen before submission ───────────────────
% DELETE the \mustdo{...} call once the action it describes has been carried out.
\newcommand{\mustdo}[1]{%
    \begin{center}
    \fboxsep=8pt\fboxrule=1.2pt%
    \fbox{\begin{minipage}{0.9\linewidth}
        \setstretch{1.15}\small
        \textbf{BEFORE SUBMISSION -- DELETE THIS BOX}\\[3pt]
        #1
    \end{minipage}}
    \end{center}%
}

% ── Hyperref LAST ─────────────────────────────────────────────────────────────
\usepackage{hyperref}
\hypersetup{
    colorlinks=false,
    pdfborder={0 0 0},
    linkbordercolor={0 0 0},
    citebordercolor={0 0 0},
    urlbordercolor={0 0 0}
}
\usepackage{cleveref}

% ══════════════════════════════════════════════════════════════════════════════
\begin{document}
\noindent
\let\cleardoublepage\clearpage

    % ── Front Matter ──────────────────────────────────────────────────────────
    \input{templatetitle}
    \input{certificate}
    \input{intro}

    % ── Chapters ──────────────────────────────────────────────────────────────
    \input{chapter/introduction}
    \input{chapter/litreview}
    \input{chapter/methodology}
    \input{chapter/systemdesign}
    \input{chapter/results}
    \input{chapter/conclusion}

    % ── References ────────────────────────────────────────────────────────────
    \newpage
    \renewcommand{\bibname}{REFERENCES}
    \bibliographystyle{IEEEtran}
    \bibliography{reference}
    \addcontentsline{toc}{chapter}{REFERENCES}

    % ── Appendix ──────────────────────────────────────────────────────────────
    \clearpage
    \appendix
    \input{appendix}

\end{document}
