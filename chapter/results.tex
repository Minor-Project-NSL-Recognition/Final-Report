\chapter{RESULT AND DISCUSSION}
\label{ch:results}

This chapter reports what the system achieves and where it fails. The dataset is described first, then the recognition results under the signer-independent protocol, then the deployment measurements, and a discussion of the remaining weaknesses.

\section{Experiment Setup}
\label{sec:experimentsetup}

All training and evaluation ran on an ordinary laptop with no GPU, under Python 3.11 with TensorFlow, MediaPipe, NumPy, and scikit-learn pinned to a mutually compatible set. The classifier has 192{,}007 parameters, and a single evaluation fold completes in minutes on the processor alone.

The random seed was fixed at 42 for every run. Fixing the seed by itself proved insufficient: TensorFlow's parallel processor reductions vary between runs, and on identical data this moved per-fold accuracy by as much as 2.7 percentage points. Deterministic operations were therefore enabled explicitly, so the accuracies below are reproducible from the same data and code, not one sample from a distribution of runs.

\section{Dataset Composition}
\label{sec:datasetcomposition}

The final dataset holds 840 clips, distributed exactly evenly at 120 clips in each of the seven classes. Table~\ref{tab:composition} gives the breakdown by class and signer.

\begin{table}[htbp]
    \centering
    \caption{Clips by class and signer in the final dataset}
    \label{tab:composition}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lcccccccc|c}
        \toprule
        \textbf{Class} & \textbf{s01} & \textbf{s02} & \textbf{s03} & \textbf{s04}
                       & \textbf{s05} & \textbf{s06} & \textbf{s07} & \textbf{s08} & \textbf{Total} \\
        \midrule
        building\_on\_fire & 20 & 20 & 20 & 20 & 10 & 10 & 10 & 10 & 120 \\
        call\_police       & 20 & 20 & 20 & 20 & 10 & 10 & 10 & 10 & 120 \\
        cant\_breathe      & 20 & 20 & 20 & 20 & 10 & 10 & 10 & 10 & 120 \\
        help\_danger       & 20 & 20 & 20 & 20 & 10 & 10 & 10 & 10 & 120 \\
        need\_ambulance    & 20 & 20 & 20 & 20 & 10 & 10 & 10 & 10 & 120 \\
        need\_toilet       & 20 & 20 & 20 & 20 & 10 & 10 & 10 & 10 & 120 \\
        none               & 20 & 20 & 20 & 20 & 10 & 10 & 10 & 10 & 120 \\
        \midrule
        \textbf{Total} & 140 & 140 & 140 & 140 & 70 & 70 & 70 & 70 & \textbf{840} \\
        \bottomrule
    \end{tabular}
\end{table}

\noindent Signers 01 to 04 are the four project members. They studied the reference recordings from the consultations of Section~\ref{sec:phase0} and rehearsed each phrase before recording, and their availability to each other let them record repeatedly, so they contributed 20 clips per class. Signers 05 to 08 are the Deaf experts consulted to establish the signed form of each phrase: Yojana Sakha and a pupil at the Rehabilitation Centre for the Rural Deaf, and two resource persons at the National Federation of the Deaf Nepal. Their time was shorter, so each contributed roughly 10 clips per class, but their signing is fluent NSL rather than a reproduction of it. Class balance across the seven classes was maintained deliberately, since an imbalanced set would have let the classifier gain accuracy by favouring whichever class was most common.

The table has no exceptions to that pattern, and this matters for reading the fold results that follow. Every signer contributes the same number of clips in every class, including \texttt{none}, so each held-out fold is scored on all seven classes in the same proportions. Fold accuracies are therefore directly comparable with one another, and the only quantity that differs between them is which person was withheld. An earlier version of this dataset was uneven in the \texttt{none} row, which made two of the eight folds measure a slightly different problem from the rest; the missing and surplus negatives were re-recorded and the results below come from the corrected set.

Eight signers is a small number, and it is the principal limitation on how much the reported accuracy can be trusted to generalise. This is discussed in Section~\ref{sec:discussion}.

\section{Sequence Length and Standardisation}

Frame counts across the 840 clips range from 30 to 217, with a median of 89.5 and a mean of 91.7. Figure~\ref{fig:seqlen} shows the distribution. The 95th percentile falls at 146 frames, which became the fixed sequence length.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{img/seqlenhist.png}
    \caption{Distribution of raw clip frame counts with 95th percentile cuttoff}
    \label{fig:seqlen}
\end{figure}

\noindent At a capture rate of 15.7 frames per second, 146 frames corresponds to about 9.3 seconds of signing, and the median clip of 89.5 frames to about 5.7 seconds. Table~\ref{tab:stdmodes} shows how the standardisation step treated the clips.

\begin{table}[htbp]
    \centering
    \caption{How the 840 clips were standardised to 146 frames}
    \label{tab:stdmodes}
    \begin{tabular}{lrp{6.4cm}}
        \toprule
        \textbf{Mode} & \textbf{Clips} & \textbf{Treatment} \\
        \midrule
        Padded ($<146$ frames)     & 794 & zero-padded at the end, with a mask recording the real frames \\
        Subsampled ($>146$ frames) & 39  & sampled at 146 evenly spaced positions \\
        Exact (146 frames)         & 7   & used unchanged \\
        \midrule
        \textbf{Total}             & \textbf{840} & \\
        \bottomrule
    \end{tabular}
\end{table}

\noindent Of the 122{,}640 frame slots in the compiled tensor, 76{,}018 hold real recorded frames and the remaining 38.0\% are zero-padding that the masking layer skips. Only 39 clips were long enough to be compressed, which matters for a reason returned to in Section~\ref{sec:discussion}: below 146 frames the model receives duration information directly in the frame count, and above it that information is normalised away.

\section{Training Behaviour}

Every fold converged and stopped on the early-stopping criterion before exhausting the 200-epoch budget. Table~\ref{tab:folds} reports where each fold stopped alongside its accuracy. The spread from 29 to 71 epochs reflects how quickly validation loss stopped improving on each training subset.

Figure~\ref{fig:curves} shows accuracy per epoch for all eight folds and Figure~\ref{fig:losscurves} the corresponding losses. Each accuracy panel carries a dashed line at that fold's held-out score. The gap between the training and validation curves, both drawn from signers inside the training set, and that dashed line is the cost of meeting a signer the model has never seen. Presenting all eight folds shows whether the folds behaved consistently, which a single representative curve cannot.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{img/trainingcurves.png}
    \caption{Accuracy per epoch for each LOSO fold.}
    \label{fig:curves}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{img/traininglosscurves.png}
    \caption{Loss per epoch for each fold. }
    \label{fig:losscurves}
\end{figure}

\section{Signer-Independent Recognition Results}

All eight signers were eligible for holdout, since removing any one of them left every class present in the remainder. Table~\ref{tab:folds} gives the result of each fold.

\begin{table}[htbp]
    \centering
    \caption{Leave-one-signer-out results.}
    \label{tab:folds}
    \begin{tabular}{lccc}
        \toprule
        \textbf{Held-out signer} & \textbf{Test clips} & \textbf{Accuracy} & \textbf{Epochs to stop} \\
        \midrule
        signer01 & 140 & 0.9429 & 31 \\
        signer02 & 140 & 0.9571 & 37 \\
        signer03 & 140 & 0.9857 & 37 \\
        signer04 & 140 & 0.9429 & 40 \\
        signer05 & 70  & 0.9714 & 44 \\
        signer06 & 70  & 0.9429 & 71 \\
        signer07 & 70  & 0.9286 & 29 \\
        signer08 & 70  & 0.9429 & 39 \\
        \midrule
        \multicolumn{2}{l}{\textbf{Mean across folds}}      & \textbf{0.9518} $\pm$ 0.0174 & \\
        \multicolumn{2}{l}{\textbf{Pooled over 840 clips}}  & \textbf{0.9536} & \\
        \bottomrule
    \end{tabular}
\end{table}

\noindent Mean accuracy across folds is 95.18\% with a standard deviation of 1.74 percentage points. Pooling all 840 predictions, each made by a model that had not seen its signer, gives 95.36\%.

The spread between folds is more informative than the mean. Figure~\ref{fig:losopersigner} shows the eight fold accuracies side by side. The best fold reaches 98.57\% and the worst 92.86\%, a range of 5.7 points, with four of the eight folds sitting together at 94.29\%. Since each fold differs only in which signer was withheld, that spread is a direct measurement of how much recognition quality depends on the individual. Reporting the mean alone would hide this; a system deployed to the public will meet signers at both ends of that range.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{img/losopersigner_cropped.png}
    \caption{Leave-one-signer-out accuracy for each held-out signer}
    \label{fig:losopersigner}
\end{figure}

No fold needs a caveat attached to it. Every signer contributes clips in all seven classes in the same proportions, so each fold is scored on the same problem and the accuracies in Table~\ref{tab:folds} can be compared against one another directly. Signer 07 at 92.86\% is the worst case and signer 03 at 98.57\% the best, and both are measured on the full seven-way task. The one fold that stands out on a different axis is signer 06, which needed 71 epochs to converge against a median of 38; it still lands on the same 94.29\% as three others, so the slower convergence cost accuracy nothing.

\subsection{Per-Class Performance}

Table~\ref{tab:perclass} gives precision, recall, and F1 for each class, pooled over all folds.

\begin{table}[htbp]
    \centering
    \caption{Per-class performance, pooled over all eight folds}
    \label{tab:perclass}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Class} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Support} \\
        \midrule
        \texttt{building\_on\_fire} & 0.9421 & 0.9500 & 0.9461 & 120 \\
        \texttt{call\_police}       & 0.9370 & 0.9917 & 0.9636 & 120 \\
        \texttt{cant\_breathe}      & 0.9661 & 0.9500 & 0.9580 & 120 \\
        \texttt{help\_danger}       & 0.9752 & 0.9833 & 0.9793 & 120 \\
        \texttt{need\_ambulance}    & 0.9590 & 0.9750 & 0.9669 & 120 \\
        \texttt{need\_toilet}       & 0.9835 & 0.9917 & 0.9876 & 120 \\
        \texttt{none}               & 0.9091 & 0.8333 & 0.8696 & 120 \\
        \midrule
        \textbf{Macro average}      & 0.9531 & 0.9536 & 0.9530 & 840 \\
        \bottomrule
    \end{tabular}
\end{table}

\noindent The six emergency phrases achieve F1 between 0.946 and 0.988. Recall on all six is at least 0.950, and for two of them, \texttt{call\_police} and \texttt{need\_toilet}, only a single clip in 120 is missed. What separates the six on F1 is mostly precision rather than recall: \texttt{call\_police} and \texttt{building\_on\_fire} sit lowest at 0.937 and 0.942 because other classes are read as them, not because they fail on their own clips. The negative class is clearly the weakest at 0.870, with a recall of 0.833.

That asymmetry is expected. The six phrases are each a specific, repeatable performance, and the model has 120 examples of each. The \texttt{none} class is not a phrase; it is everything else, sampled 120 times from an unbounded space of possible non-signs. No number of examples makes that category as learnable as the six, and its recall would be expected to remain the lowest even with far more data.

\subsection{Confusion Matrix}

Errors are easiest to read from the pooled confusion matrix in Figure~\ref{fig:confusion}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.62\textwidth]{img/confusionmatrix_cropped.png}
    \caption{Confusion matrix pooled over all eight LOSO folds.}
    \label{fig:confusion}
\end{figure}

\noindent Thirty-nine of the 840 predictions are wrong, and they fall into three groups that carry very different consequences.

Twenty errors are non-signs predicted as emergency phrases, in the bottom row. These are the most serious errors the system can make, because each one is an occasion where the system would announce an emergency that nobody signed, and they account for just over half of all the errors in the matrix. They are spread across all six phrases (four as \texttt{building\_on\_fire}, four as \texttt{cant\_breathe}, five as \texttt{need\_ambulance}, three each as \texttt{call\_police} and \texttt{help\_danger}, one as \texttt{need\_toilet}), which suggests the cause is a genuine resemblance between some non-signing motion and the opening of a phrase rather than one specific confusable pair. This single row is where the system's remaining weakness lives, and Section~\ref{sec:discussion} returns to it.

Ten errors are emergency phrases predicted as \texttt{none}, in the last column: six for \texttt{building\_on\_fire}, two for \texttt{cant\_breathe}, one for \texttt{call\_police}, and one for \texttt{need\_toilet}. Here the system stays silent when the user did sign. This leaves the user without help, though it does not produce a false alarm, and in a real deployment the user would notice the absence of a response and sign again.

Nine errors are one emergency phrase confused with another, and they fall on only four of the twenty-one possible pairs: four \texttt{cant\_breathe} clips read as \texttt{call\_police}, three \texttt{need\_ambulance} clips as \texttt{building\_on\_fire}, one \texttt{help\_danger} as \texttt{call\_police}, and one \texttt{help\_danger} as \texttt{need\_toilet}. A wrong phrase is worse than silence, because it sends the wrong help, so this is the group whose composition matters most. Two of the four pairs are directional and repeated rather than isolated: \texttt{cant\_breathe} leaks into \texttt{call\_police} and \texttt{need\_ambulance} into \texttt{building\_on\_fire}, never the reverse. Together those two pairs account for seven of the nine, and they are the only place in the matrix where confusion between phrases looks systematic rather than incidental.

Seventeen of the twenty-one phrase pairs are never confused in either direction, so phrase-to-phrase separation is largely but not entirely solved on this dataset. Distinguishing the phrases from everything that is not a sign remains the harder of the two problems, at more than twice as many errors.

\section{What the Random Split Shows}
\label{sec:leakage}

Under a stratified 70/15/15 random split, the same architecture and the same training recipe reach 97.62\% accuracy on 126 held-out clips. This is 2.3 percentage points above the pooled signer-independent figure, and 2.4 points above the mean across folds.

That gap is the point of reporting the number. Under a random split, clips from the same person appear in both training and test, so the model can score well by recognising the individual as much as the sign. The signer-independent protocol removes that possibility, and the accuracy falls. The 97.62\% figure describes how well the system recognises phrases performed by people it has already been trained on, which is not the situation any real user is in.

\begin{table}[htbp]
    \centering
    \caption{The two evaluation protocols compared}
    \label{tab:protocols}
    \setlength{\tabcolsep}{3pt}
    \begin{tabular}{lcc}
        \toprule
        \textbf{Protocol} & \textbf{Accuracy} & \textbf{What it measures} \\
        \midrule
        Random split, stratified 70/15/15 & 0.9762 & performance on known signers \\
        Signer-independent, mean of 8 folds & 0.9518 & performance on a new signer \\
        Signer-independent, pooled & 0.9536 & performance on a new signer \\
        \bottomrule
    \end{tabular}
\end{table}

\noindent This is worth stating because published NSL recognition results are generally reported without separating signers, including the closest prior landmark-based work \cite{shrestha2024nsl}. A comparison of headline accuracies across papers is therefore not meaningful unless the protocols match. On this dataset the difference between the two protocols is 2.3 points, and a project reporting only the higher number would not be doing anything unusual by the standards of the field.

\section{Generalisation and Overfitting}
\label{sec:generalisation}

A model with 192{,}007 parameters trained on 840 clips has enough capacity to memorise its
training set, so whether it has done so is a question the evaluation has to answer rather
than assume. Three accuracies are available for each fold, and the distance between them is
what the question turns on.

\begin{table}[htbp]
    \centering
    \caption{The three levels of accuracy}
    \label{tab:threelevels}
    \small
    \begin{tabular}{|p{3.3cm}|p{4.0cm}|p{5.4cm}|}
        \hline
        \textbf{Measured on} & \textbf{Drawn from} & \textbf{A large gap to the next level means} \\
        \hline
        Training clips & signers inside training & the model is fitting its own examples \\
        \hline
        Validation clips & signers inside training, clips held out & memorisation of individual clips \\
        \hline
        Held-out signer & a person absent from training & memorisation of the people, not the signs \\
        \hline
    \end{tabular}
\end{table}

\noindent Four pieces of evidence bear on overfitting.

First, no fold ran to the epoch limit. Early stopping halted every fold between 29 and 71
epochs against a budget of 200, monitoring validation loss with the best weights restored.
A model still improving on held-out data at the point it stops has not begun to memorise;
a model that needed the full 200 epochs and kept its final weights would be the worrying
case.

Second, the regularisation is not nominal. Two dropout layers at 0.3 sit between the
recurrent layers, the learning rate halves after six epochs without improvement, and the
masking layer removes padded frames from the computation so they cannot be fitted as
signal.

Third, the per-fold spread is contained. Eight independently trained models, each missing a
different signer, produce accuracies between 0.9286 and 0.9857 with a standard deviation
of 1.74 percentage points. A model that had memorised its training signers would be
expected to collapse on some folds and not others; the worst fold here is 5.7 points below
the best, not thirty. That range is still wider than the 4.0 points separating the best and
worst architectures in Section~\ref{sec:ablation}, which is the finding this chapter keeps
returning to.

Fourth, and least comfortably, the random-split comparison puts a number on the
memorisation that does remain. The 2.3-point gap between 97.62\% and 95.36\% is the
portion of a random-split score that comes from recognising people rather than phrases. It
is small, but it is not zero, and it is the reason the signer-independent figure is the one
quoted throughout this report.

Taken together these say the model generalises to a new signer with a loss of roughly two
and a quarter points against a same-signer measurement, and that it is not overfitting in the
sense of having memorised its training clips. What they do not say is that it generalises
to a new signer drawn from outside the population of eight, which is a separate claim and
is addressed in Section~\ref{sec:discussion}.

\section{Architecture Comparison}
\label{sec:ablation}

The layer sizes in Table~\ref{tab:architecture} were settled before any of the accuracies
above existed, and nothing reported so far establishes that they were the right choice. The
protocol in Section~\ref{sec:archcompare} tests them: six variants of the architecture,
each evaluated over the same eight leave-one-signer-out folds with the same seed, split, and
callbacks, so that a difference between them is a property of the architecture and not of
the training regime. The cell type is held fixed at LSTM throughout, so what varies is the
size and depth of the network and nothing else. Table~\ref{tab:ablation} gives the result and
Figure~\ref{fig:ablation} shows the same numbers as intervals.

One caveat applies to the whole table before any of its rows are read. The sweep was run on
the same corrected dataset as every other result in this chapter, but through a parallel
runner that gives each candidate its own process and a restricted thread pool, rather than
the single-process path that produced Table~\ref{tab:folds}. The shipped architecture
therefore appears twice in this chapter at slightly different values: 0.960 in
Table~\ref{tab:ablation} against 0.9518 in Table~\ref{tab:folds}. Section~\ref{sec:experimentsetup}
records why, in that TensorFlow's parallel reductions move per-fold accuracy when the thread
configuration changes. Every row of Table~\ref{tab:ablation} was produced the same way as
every other, so the comparison between architectures, which is what this section is for,
holds. The absolute values are not directly comparable against Table~\ref{tab:folds}, and no
conclusion below rests on them. The eight tenths of a point between the two measurements of
the same architecture is itself a useful floor on how finely any difference in this table can
be read.

\begin{table}[htbp]
    \centering
    \caption{Architecture variants under the LOSO protocol, ranked by mean accuracy.}
    \label{tab:ablation}
    \small
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lcccccc}
        \toprule
        \textbf{Architecture} & \textbf{Units} & \textbf{Dropout} & \textbf{Params} & \textbf{Mean} & \textbf{95\% CI} & \textbf{Epochs} \\
        \midrule
        high\_dropout          & 64/32/32   & 0.5 & 192{,}007 & 0.961 & [0.943, 0.978] & 44 \\
        \textbf{current}       & 64/32/32   & 0.3 & 192{,}007 & 0.960 & [0.943, 0.977] & 47 \\
        smaller                & 32/16/16   & 0.3 & 77{,}063  & 0.958 & [0.937, 0.979] & 45 \\
        single\_layer          & 64/--/32   & 0.3 & 152{,}839 & 0.945 & [0.912, 0.977] & 43 \\
        no\_dense\_head        & 64/32/--   & 0.3 & 190{,}151 & 0.939 & [0.900, 0.979] & 42 \\
        bigger                 & 128/64/64  & 0.3 & 535{,}559 & 0.921 & [0.874, 0.967] & 40 \\
        \bottomrule
    \end{tabular}
\end{table}


\noindent No candidate is distinguishable from any other. The means span 0.921 to 0.961, but
with only eight folds the standard error on each one is between 0.9 and 2.4 percentage
points, and every confidence interval in the table overlaps every other. Even the widest
separation in the set, between the highest-dropout variant and the largest one, is 4.0 points
against a standard error of 2.5 on the difference.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{img/architecturesweep.png}
    \caption{Mean LOSO accuracy for each architecture variant}
    \label{fig:ablation}
\end{figure}

\noindent The comparison that matters more is against the spread within each candidate. The
largest gap between any two architectures in the table is 4.0 points, while the fold-to-fold
standard deviation within a single architecture runs as high as 6.7. Which signer is held
out affects the result more than which architecture is trained. That is the substantive
finding of this section, and Section~\ref{sec:discussion} returns to it.

The clearest single reading of the table is that capacity is not the bottleneck, and past a
point it is a liability. The largest variant carries 535{,}559 parameters, 2.8 times the
shipped model, and finishes last: 3.9 points below it, with a fold-to-fold standard
deviation of 0.067, nearly three times the shipped model's 0.025, and the widest confidence
interval in the set. The smallest variant makes the same case from the other end. At 77{,}063
parameters, two fifths of the shipped model, it is 0.18 of a point behind, which is
indistinguishable. Nothing in the range from 77 thousand to half a million parameters buys
accuracy on this data, and the upper end of that range costs stability.

The two structural removals behave alike. Dropping the second recurrent layer costs 1.5
points and dropping the dense head costs 2.1, both well inside the noise on their means, but
both roughly double the fold-to-fold spread, to 0.047 and 0.057 against 0.025. Their value
is better read as consistency across signers than as accuracy, which is not something the
means alone would have shown.

One candidate the sweep does not settle is the dropout rate. Raising it from 0.3 to 0.5
changes the mean by nine hundredths of a point, the closest pair in the table, and converges
in slightly fewer epochs. The comparison establishes that 0.3 is not harmful; it does not
establish that it is better than 0.5, and this report does not claim it.

Two properties of the comparison limit what can be read from it. Each candidate was trained
once per fold from a single seed, so run-to-run variation from weight initialisation is not
measured and could account for differences of this size by itself. And eight folds cannot
resolve differences of one or two points whatever statistical test is applied to them; that
is a property of the design and not of the analysis.

The shipped architecture is therefore retained, not because it measurably outperforms the
alternatives, but because no alternative measurably outperforms it, and it sits in the range
over which accuracy has already saturated while keeping the narrowest fold-to-fold spread in
the table. Should the deployment target ever impose a size budget this model misses, the
smallest variant is the one to revisit first: two fifths of the parameters for a difference
of two tenths of a point.

\section{Why the Accuracy Is High}
\label{sec:whyhigh}

An accuracy near 95\% on a first attempt at an under-resourced sign language invites
suspicion, and it should. Five properties of the task account for it, and three of them are
forms of optimism that a reader should discount.

The legitimate reasons come first. The problem is a seven-way choice, and the six phrases
were drawn from different semantic domains: medical, fire, crime, and basic need. Nothing
constrained them to be visually distinct, but choosing by situation produced a set whose
signed forms differ in handshape, location, and movement all at once. The confusion matrix
reflects this directly: of the twenty-one possible pairs of phrases, seventeen are never
confused in either direction, and the nine phrase-to-phrase errors that do occur fall on
only four pairs. Second, the landmark representation discards background, lighting, and appearance
before the model sees anything, and dual-anchor normalisation removes position and scale.
The nuisance variation that costs a pixel-based model most of its accuracy is not present
in the input. Third, the classes are exactly balanced at 120 clips each, so no part of the
score comes from favouring a common class.

The three optimistic properties matter more for interpreting the number.

Every clip converges on a single fixed reference form, established with the consultants
before recording. This was the right methodological choice, since it fixes the ground truth
for each class, but it also means four of the eight signers are imitating a performance
rather than signing naturally, and even the four experts were asked to sign concisely and
consistently rather than with the full regional and personal variation fluent signing
normally carries. Intra-class variance here is therefore lower than a deployed system would
meet.

Each input is one complete phrase, performed deliberately, with the user marking its start
and end by tapping. The system never has to find a phrase inside continuous motion, decide
where one sign ends and the next begins, or handle a signer who changes their mind
mid-phrase. Segmentation is one of the harder problems in sign recognition and this design
avoids it entirely.

Half the signers are the authors, who knew the phrase set better than anyone and had the
most practice performing it. Their four folds average 0.9571 against 0.9464 for the other
four, a gap of 1.1 points. That gap is in the direction the explanation predicts, but it is
smaller than the standard deviation across folds and rests on four folds a side, so it does
not support a claim on its own. What it does rule out is a large practice effect: whatever
the authors' familiarity with the phrase set is worth, it is not worth much here. Note also
that the authors are the four signers with 20 clips per class, so their folds are also the
ones that remove the most training data, an effect that pushes the other way and is not
separable from the first at this sample size. The headline figure remains an accuracy on a
population half of which built the system.

None of this makes 95.36\% wrong. It makes it an accuracy on this task as specified:
seven distinct classes, one deliberate phrase at a time, performed against a fixed
reference by eight people, four of whom built the system. A wider vocabulary, natural
signing, or continuous input would each lower it.

\section{Model Export Verification}
\label{sec:exportverify}

The trained model was converted to TensorFlow Lite for deployment, reducing it from 2.3 megabytes to 881 kilobytes. That is what allows it to be carried inside the application package and run on the phone, and on the fallback server it removes the need for a full TensorFlow installation.

A conversion fault would be indistinguishable from a model that simply performs slightly worse, so the export is verified against the original instead of trusted. Both models were run on 30 real clips and their outputs compared. The largest absolute difference in any predicted probability was $1.2 \times 10^{-7}$, and the two models agreed on the predicted class for all 30 clips. The export step refuses to write its output if this check fails.

\begin{table}[htbp]
    \centering
    \caption{TensorFlow Lite export verification}
    \label{tab:export}
    \begin{tabular}{lc}
        \toprule
        \textbf{Quantity} & \textbf{Value} \\
        \midrule
        Keras model size & 2.3 MB \\
        TensorFlow Lite model size & 881 KB \\
        Clips compared & 30 \\
        Largest absolute probability difference & $1.2 \times 10^{-7}$ \\
        Predicted-class agreement & 30 of 30 \\
        \bottomrule
    \end{tabular}
\end{table}

\section{Deployment}

The delivered system is a single Android application package of about 18 megabytes, of which the two MediaPipe landmark bundles account for most and the classifier for 881 kilobytes. It carries everything recognition needs and contacts nothing. The fallback server remains available as a container that omits TensorFlow entirely and runs the exported model through a lightweight runtime, which reduces that image from about three gigabytes to one.

The cost profile of a single recognition is dominated by one operation on both paths. Landmark extraction is the whole pipeline, and everything after it (the two corrections, normalisation, standardisation, and the forward pass) is a small fixed cost paid once when the user stops signing. Because frames are landmarked while the user is still signing, the dominant cost is paid concurrently with the sign rather than after it. A design that captured a whole recording and then processed it would have serialised the two, adding several seconds of silence before every result.

The difference between the two paths is where the extraction runs, and the phone is the slower of the two. That is precisely why the temporal resampling of Section~\ref{sec:ondevice} exists: the phone cannot be made fast enough, so the clip it produces is corrected to the timescale the model expects instead.

\begin{table}[htbp]
    \centering
    \caption{Measured costs on the fallback server path}
    \label{tab:latency}
    \begin{tabular}{lc}
        \toprule
        \textbf{Stage} & \textbf{Cost} \\
        \midrule
        Landmark extraction, per frame & $\approx$ 38 ms \\
        Normalisation, standardisation, forward pass, per clip & $\approx$ 15 ms \\
        Frames retained after decimation & 15.7 per second \\
        Client send rate cap & 16 frames per second \\
        Memory per concurrent session & $\approx$ 150 MB \\
        Concurrent session limit & 4 \\
        \bottomrule
    \end{tabular}
\end{table}

\noindent Concurrency on that path is bounded by memory, not processor time, since each session holds its own MediaPipe graph. The session limit sheds load with an explicit error instead of allowing the process to be terminated for exhausting memory. The on-device path has no equivalent constraint, since each phone serves exactly one user.

The application was built and installed on a physical device, and recognition works end to end with the network disabled: the user taps to begin, performs a phrase, taps to finish, and the recognised phrase appears on screen and is spoken in Nepali from the bundled recording. The client reports its own costs as it runs, so the quantities in Table~\ref{tab:clientmeasurements} are read from the device rather than estimated.

\begin{table}[htbp]
    \centering
    \caption{On-device measurements, read from the client's own telemetry on a physical device}
    \label{tab:clientmeasurements}
    \begin{tabular}{lc}
        \toprule
        \textbf{Quantity} & \textbf{Value} \\
        \midrule
        Test device & \begin{tabular}[t]{@{}c@{}}OnePlus CPH2569, \\ Qualcomm SM7325\end{tabular} \\
        Capture resolution, source to upright & $720 \times 480$ to $480 \times 720$ \\
        Sustained landmarking rate while signing & 5.7--6.2 per second \\
        Per-frame conversion cost & 6--7 ms \\
        Per-frame landmark extraction cost & $\approx$ 165 ms \\
        Corrections, normalisation and forward pass, per clip & 70--91 ms \\
        \bottomrule
    \end{tabular}
\end{table}

\noindent The ranges are across recognitions on that handset. The per-frame extraction cost is derived from sustained throughput (frames landmarked divided by clip duration) rather than from the client's instantaneous per-frame timer, which reports only the most recent frame and so varies by more than a factor of two between consecutive readings. The derivation is corroborated by the client's own frame counters: of the frames the converter produces, roughly half are refused by the detectors because a previous frame is still in flight, which places the entire budget on landmark extraction rather than on capture or colour conversion.

Landmark extraction on the phone is therefore about four times the cost of the same operation on the server, and the sustained rate that follows from it, close to 6 frames per second, sits well below the 16 the client asks for. This is the measurement behind Section~\ref{sec:ondevice}: the gap is not a tuning problem to be closed but a property of the hardware, so the clip is resampled to the training timescale after capture instead.

\noindent Screenshots of the client recognising phrases on the device, and of the desktop recogniser, appear in Appendix~\ref{app:ui}.

\section{Development Process}

The work proceeded in nine increments, each delivering a module that could be exercised before the next began. Table~\ref{tab:sprints} records what each produced and what it changed about the plan, since several increments revised decisions made earlier.

\begin{table}[htbp]
    \centering
    \caption{Development increments}
    \label{tab:sprints}
    \footnotesize
    \begin{tabular}{|p{0.9cm}|p{4.2cm}|p{8.1cm}|}
        \hline
        \textbf{No.} & \textbf{Increment} & \textbf{Outcome and revisions to the plan} \\
        \hline
        1 & Consultation and phrase set & Signed form of each phrase fixed with Deaf experts. Face landmarks excluded after the consultants confirmed no phrase depends on expression. Phrase 6 changed to the toilet request. \\
        \hline
        2 & Recorder and pilot clips & Confirmed MediaPipe produces stable landmarks for multi-sign phrases. Established the zero-fill convention and the signer-encoded filename that made later signer-independent evaluation possible. \\
        \hline
        3 & Dataset collection and merge & 840 clips across 8 signers, merged through Git. The \texttt{none} class was added here, after it became clear a six-way classifier would name a phrase for any input. \\
        \hline
        4 & Preprocessing core & Sequence length derived from the measured distribution, and held in a metadata file rather than in code, which is what let it move as recordings were added. Established that normalisation must precede padding so padded frames stay exactly zero. \\
        \hline
        5 & Training and evaluation & Leave-one-signer-out results. Two corrections came out of this stage: the early-stopping threshold had to be raised off zero, and deterministic operations had to be enabled before fold accuracies were reproducible. The architecture comparison of Section~\ref{sec:ablation} was run here, once the evaluation harness existed to run it through. \\
        \hline
        6 & Confidence thresholding & Decision rule fixed on the softmax over all seven classes, accepting a phrase only above threshold. The confidence threshold was lowered from 0.85 to 0.75 once the negative class was in place, since 0.85 was rejecting genuine signs. A third outcome was added here: a prediction of \texttt{none} refuses rather than being announced, which the two-outcome design would have spoken aloud. \\
        \hline
        7 & Export and server deployment & TensorFlow Lite export with verification, inference server, and Android client. The deployment target changed from a browser to a phone at this stage, for the reasons in Section~\ref{sec:deploychange}. \\
        \hline
        8 & On-device recognition & Landmark extraction and classification moved onto the phone, removing the network dependency the previous increment had introduced. The preprocessing was ported and pinned to the Python implementation by generated fixtures, and two distortions specific to the phone (frame aspect ratio and an unattainable capture rate) were found and corrected. The server was retained as a fallback. \\
        \hline
        9 & Nepali output & The device text-to-speech engine was replaced by recorded Nepali audio, one clip per phrase, after a \texttt{ne-NP} voice proved absent from most devices. The recognised phrase is now displayed in Nepali as well, and the recordings are shared with the desktop demonstration. \\
        \hline
    \end{tabular}
\end{table}

\section{Behaviour as the Vocabulary Grows}
\label{sec:scaling}

The system recognises seven classes. Any extension of this work adds more, so it is worth
setting out what the design predicts will happen, and which parts of it break first. No
experiment in this report tests this; what follows is an argument from the structure of the
method, and it identifies where the next measurements should be taken.

\subsection{Confusion Grows Faster Than the Vocabulary}

A $K$-class problem has $K(K-1)/2$ pairs that can be confused. At seven classes there are
21, and the confusion matrix shows four of them carrying any error at all, two of those
more than a single one. The count grows quadratically: 105 pairs at fifteen classes, 435
at thirty.

The more important effect is not the arithmetic but which pairs get added. The six phrases
here were chosen by situation, not by appearance, and they happened to differ in handshape,
location, and movement simultaneously. A vocabulary of thirty NSL phrases cannot be chosen
that way. It must include pairs that differ in one parameter only, which is what
sign-language phonology calls a minimal pair, and those are exactly the cases a
146-frame landmark sequence at 15.7 frames per second is least able to separate. Two signs
distinguished by a brief change in handshape occupy a handful of frames out of 146.

The exclusion of facial landmarks becomes a limitation at this point rather than a
simplification. The consultants confirmed that none of the six current phrases depends on
facial expression, and that confirmation does not extend to a larger set. NSL, like other
sign languages, carries grammatical information on the face, including negation and
question marking. A vocabulary that includes a phrase and its negation cannot be separated
by the current 225-value feature vector at all, because the information that distinguishes
them is discarded before the model sees the input.

\subsection{The Data Requirement Scales With Classes, Not Total Clips}

What governs recognition quality is clips per class, not clips overall. Holding the present
120 per class, a thirty-class vocabulary needs 3{,}600 clips, and each new class needs its
own consultation to establish its signed form before recording can begin. The consultation
step, not the recording, is the binding constraint: it requires expert time from a small
community.

\subsection{The Negative Class Gets Harder}

The \texttt{none} class is already the weakest at 0.870 F1, and it degrades as the
vocabulary grows for a structural reason. Its job is to cover everything that is not one of
the known phrases. As known phrases multiply, the boundary between the known region and
everything else becomes longer and more intricate, while the number of \texttt{none} clips
available to describe it stays fixed. A negative class of 120 clips describing the
complement of six phrases is thin; describing the complement of thirty is thinner.

\subsection{The Confidence Threshold Needs Recalibrating}

The threshold of 0.75 requires the model to place three quarters of its probability mass on
one class. Softmax spreads probability across whatever the model finds plausible, so as
near-neighbours multiply the maximum probability falls even when the top prediction is
correct. A fixed 0.75 would therefore reject an increasing share of correct recognitions as
\texttt{low\_confidence}. The threshold is a function of vocabulary size and would have to
be recalibrated on validation data at each vocabulary, not carried forward.

\subsection{Predicted Shape of the Degradation}

Table~\ref{tab:scaling} collects these effects.

\begin{table}[htbp]
    \centering
    \caption{Predicted effect of a larger vocabulary on each component}
    \label{tab:scaling}
    \small
    \begin{tabular}{|p{3.5cm}|p{4.3cm}|p{4.9cm}|}
        \hline
        \textbf{Component} & \textbf{Effect of raising $K$} & \textbf{What would be needed} \\
        \hline
        Phrase classifier & Confusable pairs grow as $K^2$; minimal pairs enter the set & Higher frame rate, or explicit handshape features \\
        \hline
        Feature vector & Face-dependent distinctions become unrepresentable & Add the face block, at roughly sevenfold the input width \\
        \hline
        Training data & 120 clips per new class, plus one consultation each & Expert time, the scarcest input \\
        \hline
        \texttt{none} class & Describes a larger complement with the same clip count & Negative clips scaled with $K$ \\
        \hline
        Confidence threshold & Correct predictions fall below a fixed 0.75 & Recalibration per vocabulary size \\
        \hline
    \end{tabular}
\end{table}

\noindent The expected trajectory is therefore not a steady decline. Adding phrases that are
semantically and visually distinct from the existing six should cost little, because the
representation already separates such phrases well. Accuracy should fall sharply at the
point where the vocabulary first admits a minimal pair, and again where it admits a
face-dependent distinction, since the second of those is not a matter of degree but of
information the input does not contain.

The cheapest way to test this is to add three or four phrases chosen to be deliberately
similar to existing ones and re-run the leave-one-signer-out evaluation. That measures the
first of the two cliffs directly, at a cost of one consultation and a few hundred clips.

\section{Discussion}
\label{sec:discussion}

\subsection{What the Results Support}

Under a protocol that tests only on unseen signers, the system recognises the six emergency phrases with a pooled accuracy of 95.36\% and per-class F1 between 0.946 and 0.988. The landmark representation is what makes this achievable from 840 clips: a pixel-based model on the same data would have far more parameters to fit and far less invariance to the rooms and lighting the clips were recorded in. It is also what makes the result deployable, since a classifier over 225 coordinates is small enough to run on the phone that captured them.

The evaluation protocol is the part of this work most worth carrying forward. The 2.3-point gap between the random split and the signer-independent folds is a concrete measurement of how much a random split flatters a model on this kind of data, and it was obtained on a dataset small enough that the effect could easily have been dismissed as noise.

\subsection{Eight Signers Is the Binding Limitation}

Every accuracy in this chapter rests on eight people, four of whom are the authors. The standard deviation across folds is 1.74 points and the range is 5.7, which means the estimate of performance on a new signer is itself uncertain. A ninth signer with an unusual signing style could plausibly score below any fold reported here.

The architecture comparison in Section~\ref{sec:ablation} puts this on a firmer footing than an assertion. Across six architectures spanning 77{,}063 to 535{,}559 parameters, no variant is distinguishable from any other, and the widest gap between any two of them is narrower than the range across held-out signers within a single one. What limits this system is not the model that was chosen but the number of people it has been measured on.

Four of the eight signers are also not fluent NSL users. Signers 01 to 04 are the project members, who learned each phrase from the consultants' reference and reproduced it; signers 05 to 08 are the Deaf experts who set that reference in the first place. The dataset therefore contains a mixture of fluent and learned performances, and the accuracy above is an accuracy on that mixture, not on fluent signing alone.

\subsection{The Negative Class Is the Weak Point}

The \texttt{none} class carries a recall of 0.833, and twenty non-signs were predicted as emergency phrases. In deployment these are the errors that produce a false announcement, which is the most serious failure mode this system can have. They are also just over half of every error in the matrix, and more than twice the nine errors that confuse one phrase with another: what remains hardest is the boundary between the phrases and everything that is not one.

That single row therefore accounts for most of the difference between the system's accuracy and the accuracy it could have. If every non-sign were correctly rejected, pooled accuracy would rise from 95.36\% to 97.7\%.

Reducing this error is a question of what the negative class covers rather than of the threshold it is checked against. The \texttt{none} class holds 120 clips describing the complement of six phrases (an unbounded space sampled 120 times), and it is the only class whose examples are not a specific repeatable performance. Widening it with more varied non-signing motion, particularly the kind of motion these twenty clips resemble, is the direct way to give the classifier a better boundary to place, and it is the single most useful addition to this evaluation. How many of the twenty cleared the confidence threshold is not recorded in the saved metrics and would need measuring separately; that number decides whether a stricter threshold would help at all, and it should be obtained before any threshold is changed.

\subsection{Duration Is Only Encoded Below the Sequence Length}

The model has no explicit time axis and reads frame count as duration, which is why both deployment paths correct the capture rate before classifying. That reasoning holds for clips shorter than 146 frames, which is 794 of the 840. For the 39 longer clips, uniform subsampling compresses the performance to look exactly 146 frames long, so their duration information is removed. Anything longer than about 9.3 seconds of signing is therefore scale-normalised and not measured. This is an acceptable engineering choice at this vocabulary size, and it would need revisiting for longer phrases.

A related effect follows from the masking layer. A frame in which MediaPipe detected neither the pose nor either hand is all zeros, and is therefore indistinguishable from padding and skipped. A clip containing many undetected frames is consequently shorter, from the model's point of view, than its frame count suggests.

\subsection{What Moving On-Device Cost}

Recognition runs on the phone, so the delivered system works with no network connection at all. That was the right outcome for an emergency tool, but it was not free, and the price is worth stating precisely because it is a weakness that does not show up in any accuracy figure.

The measured 95.36\% was obtained on landmarks produced by MediaPipe Holistic through a webcam, preprocessed by one module. The phone produces landmarks with a different extractor, through a different lens, at a different frame rate, and preprocesses them with a port of that module rather than the module itself. Nothing in this chapter measures the accuracy of that combination. Two of the differences were large enough to find and correct (the frame aspect ratio and the capture rate, both described in Section~\ref{sec:ondevice}), and both were found only because they broke recognition visibly. A smaller discrepancy would not have announced itself; it would have shown up as a few more errors, indistinguishable from the model simply being imperfect.

Three things reduce that exposure without removing it. The ported preprocessing is pinned to the Python implementation by fixtures generated from it, so an arithmetic divergence fails a test. The client writes the raw landmark clip behind every attempt to storage, so any suspect result can be replayed through the Python pipeline afterwards, which is how both corrections were diagnosed. And the fallback server path remains, running the original module, so the two can be compared on the same signer at any time.

What is still missing is the measurement that would close the question: a set of clips captured through both extraction paths at once, from which the on-device accuracy could be estimated directly rather than argued for. The harness for it exists in the codebase and the recordings have not been made. It is the first item in the future work of Chapter~\ref{ch:conclusion}.
